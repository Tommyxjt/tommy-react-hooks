import useForceUpdateInternal from '../_internal/react/useForceUpdate';

/**
 * useForceUpdate（Public）
 *
 * 强制触发一次 rerender。
 */
const useForceUpdate = useForceUpdateInternal;

export default useForceUpdate;
