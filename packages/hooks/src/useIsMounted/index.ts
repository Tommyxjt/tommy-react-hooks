import useIsMountedInternal from '../_internal/react/useIsMounted';

/**
 * useIsMounted（Public）
 *
 * 返回 () => boolean，用于判断组件当前是否仍 mounted。
 */
const useIsMounted = useIsMountedInternal;

export default useIsMounted;
